FROM python:3.11-slim

WORKDIR /app

# Copy only what's needed for the health check
COPY railway_deploy.sh .
RUN chmod +x railway_deploy.sh

# Expose the port that Railway will use
EXPOSE 8080

# Start with the minimal health check script
CMD ["bash", "railway_deploy.sh"] 